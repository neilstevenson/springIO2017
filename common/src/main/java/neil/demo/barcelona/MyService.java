package neil.demo.barcelona;

/**
 * <P>Define what the Fibinacci service must do, then we
 * can provide progressively smarter implementations in
 * the modules.
 * </P>
 */
public interface MyService {

	public long fibonacci(long l);
	
}
